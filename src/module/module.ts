

// const add = (num1: number, num2: number): number => {
//     return num1 + num2
// }

// const substrac = (param1: number, param2: number): number => {
//     return param1 - param2
// }

// const multiply = (num1: number, num2: number): number => {
//     return num1 * num2
// }

const avarage = (num1: number, num2: number): number => {
    return num1 + num2 / 2
}

export default {

    //     multiply,
    //     substrac,
    //     add,
    avarage
};
